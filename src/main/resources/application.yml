spring:
  application:
    name: openstack
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/openstack?characterEncoding=utf-8&&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
server:
  port: 8000
network:
  type: LOCAL,FLAT,VLAN,VXLAN,GRE

mybatis:
  mapper-locations: classpath:/mapper/*.xml


